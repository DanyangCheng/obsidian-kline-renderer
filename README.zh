# 📈 Obsidian K-Line Renderer
Obsidian K-Line Renderer 是一款为 Obsidian 量身打造的本地 K线图（蜡烛图）渲染插件。它允许你直接在 Markdown 笔记中，通过简单的代码块语法，将历史行情数据、量化回测结果或金融分析数据转化为高度互动、完全本地化的专业 SVG 图表。

## ✨ 核心特性
- 🖱️ 深度交互体验：

   缩放：在图表区域使用鼠标滚轮（或触控板缩放）动态调整可视时间轴。

   平移：按住鼠标左键左右拖拽，平滑浏览历史数据。

   智能提示：鼠标悬浮自动吸附最近的 K 线，显示具体的 OHLC 及成交量数据。

- 🎨 高度可定制：支持全局设置面板（颜色、尺寸、均线等），同时也支持在单个代码块中通过 # key: value 语法进行局部覆盖。

- 📊 多种数据格式兼容：支持简易空格分隔、CSV 格式以及 JSON 数组格式。

## 📦 安装方法
(注：待插件上架 Obsidian 社区插件市场后，可通过市场直接搜索安装。目前请使用手动安装方式)

### 手动安装 (开发者推荐)
下载本仓库的最新 Release 发布包。

将解压后的文件夹（包含 main.js, manifest.json, styles.css）放入你的 Obsidian 插件目录：你的仓库路径/.obsidian/plugins/obsidian-kline-renderer/

重启 Obsidian，在“第三方插件”设置中启用 K-Line Renderer。

## 📖 使用指南
在 Obsidian 笔记中创建一个 kline 代码块，输入你的数据即可渲染图表。

### 基础语法：简易文本模式
你可以直接输入以空格、逗号或制表符分隔的数据（格式：日期/标签, 开盘价, 最高价, 最低价, 收盘价, [成交量]）。

```kline
2024-01-01 100 110 95 105
2024-01-02 105 115 100 112 5000
2024-01-03 110 120 108 118 6200
2024-01-04 115 116 105 108 4100
```
### 进阶语法：局部配置与带表头的 CSV
你可以使用 # key: value 的注释语法为当前图表设置专属样式，这会覆盖插件的全局设置。


```kline
# title: 苹果公司 (AAPL) 历史走势
# width: 900
# height: 450
# volume: true
# bull: #26a69a
# bear: #ef5350

date, open, high, low, close, volume
2024-01-01, 150.2, 155.0, 149.5, 154.1, 100000
2024-01-02, 154.5, 156.2, 152.0, 153.8, 85000
2024-01-03, 153.0, 158.5, 152.5, 158.0, 120000
```
支持的局部配置项：

- title: 图表顶部标题

- width / # height: 图表宽高（像素）

- bull / # bear: 阳线和阴线的十六进制颜色

- volume: true 或 false，是否显示底部成交量

- grid: true 或 false，是否显示背景网格


### JSON 数据流
如果你使用 Python 或 Node.js 编写量化脚本，可以直接将导出的 JSON 数组粘贴进来，插件会自动解析。

```kline
# title: JSON 数据源测试
[
  {"label":"24-01", "open":10, "high":12, "low":9, "close":11, "volume": 100},
  {"label":"24-02", "open":11, "high":15, "low":10, "close":14, "volume": 150}
]
```

## ⚙️ 插件设置
在 Obsidian 的插件设置页面中，你可以配置全局的渲染偏好：

- 尺寸设置：默认的代码块渲染宽度和高度。

- 颜色设置：全面自定义涨跌颜色（Bull/Bear Color）、背景色、坐标轴文本与网格颜色。

- 显示控制：全局控制是否默认显示成交量、网格线和交互式信息提示（Tooltip）。

## 🛠️ 开发者指南
目录架构
```
src/
 ├── types.ts            # 核心接口与类型定义
 ├── constants.ts        # 全局默认配置
 ├── parser.ts           # 纯函数数据解析器 (Text/JSON -> KlineData)
 ├── renderer/
 │    ├── svg.ts         # 无状态 SVG 渲染引擎
 │    └── interaction.ts # 视图状态控制与 DOM 事件监听 (解决内存泄漏)
 ├── settings.ts         # Obsidian 设置面板 UI
 └── main.ts             # 插件生命周期与代码块处理器注册
```

## TOD

接入行情API，自动获取行情数据。